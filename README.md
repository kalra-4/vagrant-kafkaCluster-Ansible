# vagrant-kafkaCluster-Ansible

## Requirements

- Vagrant
- Ansible

## Usage

### Setup

Clone this repository.

git clone 
cd


### Execute

Start vagrant VMs and provision by using ansible.

    $ vagrant up

(After the first time) Execute ansible playbook.

    $ vagrant provision
